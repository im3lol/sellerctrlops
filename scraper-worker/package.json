{
  "name": "sellerctrl-scraper-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dockerised Playwright worker for SellerCtrl: polls the app for scrape jobs, extracts product data via saved selectors, posts it back.",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "playwright": "1.49.1"
  }
}
